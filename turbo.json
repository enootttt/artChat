{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "build.zip"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
